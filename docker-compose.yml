version: "3.3"
services:
        webapp:
                build: .
                ports:
                        - "8080:8080"
                depends_on:
                        - db
        db:
                image: yottadb/yottadb-debian
                volumes:
                        - server-db-data:/data

volumes:
        server-db-data:
